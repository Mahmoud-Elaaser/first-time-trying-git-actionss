on:
  push:
    branches:
      - master




jobs:
  build:
    runs-on: ubuntu-latest

    steps: 
      - name: 'Before'
        run: ls

        # - name: 'Testing Printing'
        # run: echo Mahmoud

      - uses: actions/checkout@v3

      - name: 'After'
        run: ls

      - name: "Login to Docker hub"
        run: docker login --username ma7moud22 -- password dckr_pat_TOCJEj6WpJ131gVsC3zr9E-0K4Q